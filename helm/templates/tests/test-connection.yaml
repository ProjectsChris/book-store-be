apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "book-store-be.fullname" . }}-test-connection"
  labels:
    {{- include "book-store-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "book-store-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
