apiVersion: v1
kind: Service
metadata:
  name: {{ include "book-store-be.fullname" . }}
  labels:
    {{- include "book-store-be.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "book-store-be.selectorLabels" . | nindent 4 }}
